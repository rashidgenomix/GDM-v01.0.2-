
import 'package:flutter/material.dart';

class ExperimentLayout extends StatelessWidget {
  const ExperimentLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Experiment layout planner UI."),
    );
  }
}
