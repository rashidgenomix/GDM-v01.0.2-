
import 'package:flutter/material.dart';

class Descriptors extends StatelessWidget {
  const Descriptors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Trait descriptors by crop and organization."),
    );
  }
}
