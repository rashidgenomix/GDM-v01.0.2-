
import 'package:flutter/material.dart';

class DataEntry extends StatelessWidget {
  const DataEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Data entry table with add/edit rows goes here."),
    );
  }
}
