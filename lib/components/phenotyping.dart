
import 'package:flutter/material.dart';

class Phenotyping extends StatelessWidget {
  const Phenotyping({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Phenotyping form and records go here."),
    );
  }
}
